// examples/demo.ltl — runnable demo for ltl_forth_lite

import std::io
use ltl_forth_lite as lib

fn main() {
    io::println("ltl_forth_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}