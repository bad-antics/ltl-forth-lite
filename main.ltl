// ltl-forth-lite/main.ltl
//
// Indirect-threaded Forth interpreter with IMMEDIATE words.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-forth-lite v0.1.0")
    io::println("Indirect-threaded Forth interpreter with IMMEDIATE words.")
    0
}
